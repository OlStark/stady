'use strict';

    function two(y){
        
    let x = 56;

        function one(x){
           
            return x < y;
    }
    return one(+prompt('Угадай число от 1 до 100'));
}

console.log(two(false = alert('Ввели неверное число')));

